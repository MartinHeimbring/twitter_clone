
<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render @feed_items %> <!-- could also be replaced by 'current_user.micropost.all' and it'll still work -->
  </ol>
  <%= will_paginate @feed_items %>
<% end %>